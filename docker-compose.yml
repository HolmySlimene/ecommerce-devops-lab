version: "3"

services:

  mongo:
    image: mongo
    container_name: mongo
    ports:
      - "27017:27017"

  app:
    build: ./app
    container_name: node-app
    ports:
      - "3000:3000"
    depends_on:
      - mongo
